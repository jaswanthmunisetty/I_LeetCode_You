class Solution {
    public boolean uniformArray(int[] nums) {
        return true;
        // nee yabba sanka naaka kadhara solve cheyaniki, return true endhi ra ungamma, modda lo leetcode!
        // int count = 0;

        // for (int num : nums) {
        //     if (num % 2 != 0) {
        //         count++;
        //     }
        // }

        // if (count == 0 || count >= 2) {
        //     return true;
        // }

        // return false;
    }
}